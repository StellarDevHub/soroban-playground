[package]
name = "soroban-voting-example"
version = "0.1.0"
edition = "2021"
description = "A simple voting contract example for Soroban"


[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.6"

[dev-dependencies]
soroban-sdk = { version = "21.0.6", features = ["testutils"] }

