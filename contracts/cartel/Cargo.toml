[package]
name = "cartel-syndicate"
version = "0.1.0"
edition = "2021"
description = "Syndicate/DAO-style staking and proposal voting contract"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.6"

[dev-dependencies]
soroban-sdk = { version = "21.0.6", features = ["testutils"] }